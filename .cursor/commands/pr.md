Take my current working changes and:

1. Break them into small, semantic commits grouped by concern (types, component logic, tests, stories — separate commits each).
2. Push to a feature branch named `feat/<short-description>`.
3. Open a PR with a description covering: what changed, what tests cover it, any risk notes for the reviewer, and a screenshot if Storybook output changed.
